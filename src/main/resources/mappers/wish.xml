<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.happyhouse.mapper.WishDao">
	<select id="wishList" parameterType="string" resultType="wishDto">
			select userId, wishlist.AptName as AptName, dong, buildYear from wishlist
			LEFT JOIN houseinfo ON wishlist.AptName = houseinfo.AptName
			where userId = #{userId}
	</select>
	
	<select id="checkWish" parameterType="string" resultType="int">
			select count(*) from wishlist LEFT JOIN houseinfo ON wishlist.AptName = houseinfo.AptName where userId = #{userId} and wishlist.AptName = #{AptName};
	</select>
	
	<insert id="registWish" parameterType="string">
		insert into wishlist (userId, AptName)
		values (#{userId}, #{AptName})
	</insert>	
	
	<delete id="deleteWish" parameterType="string">
		delete from wishlist
		where userId = #{userId} and AptName = #{AptName}
	</delete>
</mapper>